# Install by running `bower install`
bower_components/

# Install by running `npm install`
node_modules/

# Install by running `./composer.phar update`
vendor/

# Run `gulp` to generate the build directory.
build/

# Run `gulp dist` to generate the distribution directory.
dist/